-------------------------Setting up project structure---------------------------

1. Create repo, clone it in local
2. Create a virtual environment named 'galaxias' - conda create -n galaxias python=3.10
3. Activate the virtual environment - conda activate galaxias
4. python -m pip install cookiecutter
5. cookiecutter -c v1 https://github.com/drivendata/cookiecutter-data-science
6. Rename src.models -> src.model
7. git add - commit - push

-------------------------MLFlow setup on Dagshub---------------------------
8. Go to: https://dagshub.com/dashboard
9. Create > New Repo > Connect a repo > (Github) Connect > Select your repo > Connect
10. Copy experiment tracking url and code snippet. (Also try: Go To MLFlow UI)
11. python -m pip install dagshub
    python -m pip install mlflow
12. Run the exp notebooks
13. git add - commit - push
14. dvc init
15. create a local folder as "local_s3" (temporary work)
16. on terminal - "dvc remote add -d mylocal local_s3"

17. Add code to below files/folders inside src dir:
    - logger
    - data_ingestion.py
    - data_preprocessing.py
    - feature_engineering.py
    - model_building.py
    - model_evaluation.py
    - register_model.py
18. add file - dvc.yaml (till model evaluation.metrics)
19. add file - params.yaml
20. DVC pipeline is ready to run - dvc repro
21. Once done - dvc status
22. git add - commit - push
23. Need to add S3 as remote storage - Create IAM User(keep cred) and S3 bucket
24. pip install - dvc[s3] & awscli
25. Checking/deleting dvc remote (optional) - [dvc remote list & dvc remote remove <name>] 
26. Set aws cred - aws configure
27. Add s3 as dvc remote storage - dvc remote add -d myremote s3://<bucket-name>
    e.g.: dvc remote add -d myremote s3://sentiment-analysis-mlops-vk
    dvc status
    dvc commit
    dvc push

28. Create new folder: "flask_app" | Inside that, add directory & rest of the files
29. "python -m pip install flask" and run the app (optional: dvc push - to push data to S3)
30. pip freeze > requirements.txt
31. Add .github/workflows/ci.yaml file
32. Create key token on Dagshub for auth: Go to dagshub repo > Upper right corner> profile > Your settings > Tokens > Generate new token
    >> Please make sure to save token << >> capstone_test: xxxxx<<
    >> Add this auth token to github secret
        Project github repo > Setttings > Secrets & Variables > Actions > Under Repository secrets "New Repository secret"
    Update on ci file
31. Add dir "tests"&"scripts" and files within. This will contain our test related scripts for CI.
    git add, commit. push to test the github actions pipeline.

>>>>> Moving to Docker <<<<<
32. pip install pipreqs
33. cd flask_app & do "pipreqs . --force"
34. Add dockerfile and start docker-desktop in background
    Also before proceeding make sure: [switch the mlflow server setup to param version, change cmd on dockerfile]
35. go to root dir and: "docker build -t sentimentanalysis-app:latest ."
36. Try running the image: "docker run -p 8888:5000 sentimentanalysis-app:latest"
    - This run will give 'OSError: capstone_test environment variable is not set'...obviously
    - alternate: docker run -p 8888:5000 -e CAPSTONE_TEST=7afc547c2c36d8e25e6547a1ef82651aa3843d8b sentimentanalysis-app:latest
    - docker push youruser/sentimentanalysis-app:latest (optional)
    - optional: try to delete image locally and pull it from dockerhub and run (optional)
37. Setup aws services for below secrets and variables:
	AWS_ACCESS_KEY_ID
	AWS_SECRET_ACCESS_KEY
	AWS_REGION
	ECR_REPOSITORY (sentiment-analysis-mlops)
    AWS_ACCOUNT_ID
   (Also add this permission to the IAM user: AmazonEC2ContainerRegistryFullAccess)
38. Execute CICD pipeline till the stage where we build and push image to ECR.

----------------------------------------------------------------------------------
*********Setup required before moving to EKS deployment*********
----------------------------------------------------------------------------------
* Run the following in PowerShell to see the AWS CLI path being used: Get-Command aws
If the path points to your Anaconda environment (e.g., C:\Users\Personal\anaconda3\...), it’s a conflicting installation.

* Uninstall Python-Based AWS CLI(Remove the conflicting AWS CLI from your Python environment): python -m pip uninstall awscli
* Verify it's uninstalled: aws --version
* Update Environment Variables:
> Make sure the .msi AWS CLI path is in your system PATH (usually installed at C:\Program Files\Amazon\AWSCLIV2\aws.exe)
> Add it to your PATH if missing: Open Control Panel > System > Advanced System Settings > Environment Variables. Under "System Variables," find Path, and add the AWS CLI path: C:\Program Files\Amazon\AWSCLIV2\
> Test AWS CLI Again: aws --version
----------------------------------------------------------------------------------

* Download kubectl using below link:
    https://kubernetes.io/docs/tasks/tools/install-kubectl-windows/#install-kubectl-binary-on-windows-via-direct-download-or-curl
* Move kubectl.exe to a directory in your system PATH, such as C:\Windows\System32
* Test if kubectl is properly installed: kubectl version --client

* Download eksctl using below link:
    https://docs.aws.amazon.com/eks/latest/eksctl/installation.html
* Extract eksctl & Move eksctl.exe to a directory in your system PATH, such as C:\Windows\System32
* Test if eksctl is properly installed: eksctl version
----------------------------------------------------------------------------------
* Verify AWS CLI: aws --version
* Verify kubectl: kubectl version --client
* Verify eksctl: eksctl version
----------------------------------------------------------------------------------

39. Create an EKS cluster:
        eksctl create cluster --name sentiment-app-cluster --region ap-south-1 --nodegroup-name sentiment-app-nodes --node-type t3.small --nodes 1 --nodes-min 1 --nodes-max 1 --managed
40. Update kubectl Config(Once the cluster is created, eksctl will automatically update your kubectl config file. However, you can verify and set it manually using:)
        aws eks --region ap-south-1 update-kubeconfig --name sentiment-app-cluster (This ensures your kubectl is pointing to the correct cluster.)
41. Check EKS Cluster Configuration Ensure you can access your EKS cluster by running
        aws eks list-clusters
42. Delete cluster(optional):
        eksctl delete cluster --name sentiment-app-cluster --region ap-south-1
    Also, verify cluster deletion:
        eksctl get cluster --region ap-south-1
43. Verify the cluster status:
    aws eks --region ap-south-1 describe-cluster --name sentiment-app-cluster --query "cluster.status"
44. Check cluster connectivity:
        kubectl get nodes
45. Check the namespaces:
        kubectl get namespaces
46. Verify the deployment:
        kubectl get pods
        kubectl get svc
47. Deploy the app on EKS via CICD pipeline 
        >> edit ci.yaml, deployment.yaml, dockerfile
            Modify deployment.yaml file to update app name, ECR image name as per your setup.
        >> Also edit the security group for nodes and edit inbound rule for 5000 port
48. Once the LoadBalancer service is up, get the external IP:
        kubectl get svc sentiment-app-service
49. Try external-ip:5000 directly on url or on terminal :
        curl http://aecbfc152edfc464a9d740c9fd9f9911-1951070685.ap-south-1.elb.amazonaws.com:5000


>>>>>>>>>> Prometheus Server Setup <<<<<<<<<<

50. Launch an Ubuntu EC2 Instance for Prometheus:
    t3.medium, 8 GB of disk space (general-purpose SSD)
    Security Group: Allow inbound access on ports: 9090 for Prometheus Web UI, 22 for SSH access
51. SSH into the EC2 Instance(optional or connect directly to ec2 server alternatively):
    ssh -i your-key.pem ubuntu@your-ec2-public-ip
52. Update packages:
    sudo apt update && sudo apt upgrade -y
53. Download Prometheus:
    wget https://github.com/prometheus/prometheus/releases/download/v2.46.0/prometheus-2.46.0.linux-amd64.tar.gz
    tar -xvzf prometheus-2.46.0.linux-amd64.tar.gz
    mv prometheus-2.46.0.linux-amd64 prometheus
54. Move files to standard paths:
    sudo mv prometheus /etc/prometheus
    sudo mv /etc/prometheus/prometheus /usr/local/bin/
55. Create Prometheus Configuration: 
>> Open the file for editing: sudo nano /etc/prometheus/prometheus.yml
>> Edit the File:

global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "sentiment-app"
    static_configs:
      - targets: ["aecbfc152edfc464a9d740c9fd9f9911-1951070685.ap-south-1.elb.amazonaws.com:5000"]  # Replace with your app's External IP

>> Save the File: ctrl+o -> enter -> ctrl+x
>> Verify the Changes: cat /etc/prometheus/prometheus.yml

56. Locate the Prometheus Binary(Run the following command to find where the prometheus executable is installed):
    which prometheus
    T  his should return the full path to the prometheus binary, such as /usr/local/bin/Prometheus
57. Run Prometheus with the config file:
    /usr/local/bin/prometheus --config.file=/etc/prometheus/prometheus.yml
58. optional:
    To run prometheus as systemd service, use below steps:
    sudo nano /etc/systemd/system/prometheus.service
    Add below contents to file:
[Unit]
Description=Prometheus Monitoring System
Wants=network-online.target
After=network-online.target

[Service]
User=root
ExecStart=/usr/local/bin/prometheus \
  --config.file=/etc/prometheus/prometheus.yml \
  --storage.tsdb.path=/etc/prometheus/data

Restart=always

[Install]
WantedBy=multi-user.target

    >> Save the File: ctrl+o -> enter -> ctrl+x

    sudo systemctl daemon-reload
    sudo systemctl status prometheus
        You should see:
            Active: active (running)
    Enable auto-start after EC2 reboot
        sudo systemctl enable prometheus
    Verify port 9090
        sudo ss -tulpn | grep 9090


>>>>>>>>>> Grafana Server Setup <<<<<<<<<<

59. Launch an Ubuntu EC2 Instance for Grafana:
    t3.medium,  8 GB of disk space (general-purpose SSD)
    Security Group: Allow inbound access on ports: 3000 for Grafana Web UI, 22 for SSH access
60. SSH into the EC2 Instance(optional or connect directly to ec2 server alternatively):
    ssh -i your-key.pem ubuntu@your-ec2-public-ip
61. Update and upgrade system packages:
    sudo apt update && sudo apt upgrade -y
62. Download Grafana: wget https://dl.grafana.com/oss/release/grafana_10.1.5_amd64.deb
    (this is a stable version for now; adjust the link if necessary.)
63. Install Grafana:
    sudo apt install ./grafana_10.1.5_amd64.deb -y
64. Start the Grafana service:
    sudo systemctl start grafana-server
65. Enable Grafana to start on boot:
    sudo systemctl enable grafana-server
66. Verify the service is running:
    sudo systemctl status grafana-server
67. Open Grafana web UI:
    http://<ec2-public-ip>:3000 (username/pwd - admin)
68. Add Prometheus as a Data Source:
    e.g.: http://public-ip/:9090 or http://private-ip/:9090 based on your Security Group configurations.
    click - Save and Test | Get started with building dashboards.

----------------------------------------------------------------------------------

AWS Resource Cleanup:

* Delete deployment - kubectl delete deployment sentiment-app
* Delete service - kubectl delete service sentiment-app-service
* Delete env var - kubectl delete secret capstone-secret
* Delete EKS Cluster - eksctl delete cluster --name sentiment-app-cluster --region ap-south-1
* Verify Cluster Deletion - eksctl get cluster --region ap-south-1
* Delete artifacts of ECR and S3 (optional - delete ECR and S3)
* Validate if Cloud Formation stacks are deleted.
* Confirm service termination on AWS support chat.
----------------------------------------------------------------------------------

conda env export --no-builds > environment.yml
conda env create -f environment.yml

Thank You.


